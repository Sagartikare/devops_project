- name: Deploy Docker container
  hosts: localhost
  connection: local
  tasks:
    - name: Stop old container if running
      command: docker rm -f myapp || true

    - name: Run new container
      command: docker run -d -p 5000:5000 --name myapp myapp
